# 냉로그

구매 기록을 냉장고 재고로 바꾸고 무엇을 언제 먹을지 안내하는 모바일 웹앱.
관리의 번거로움을 줄이고 구매 → 확인 → 보관 → 소비 흐름을 완성한다.
목표 사용자는 식재료 기록과 수량 관리가 번거로운 1~2인 가구다.
Wanted AI Championship 2026 제출 수준의 실제 동작 서비스가 목표이며, 대회 세부 규정은 아직 확인하지 않았다.

## 현재 범위
외부 AI API, 키 발급, 결제 및 모델 선택은 금지한다. Mock Provider와 결정적 로직만 사용하고 이를 화면에서 알린다.
MVP: 이미지/직접 구매 등록, 확인 편집, 재고, 보관별 예상 시점, 우선순위와 브리핑, 자연어 소비 및 보관 변경, 저장, 배포.
메뉴는 간단한 행동 제안으로 한정. 폐기 기록은 거래에 포함. 통계 고도화, 알림, PWA는 후순위. 가족 공유·쇼핑몰 연동·커뮤니티는 제외한다.

## 상태 및 우선순위
2026-09-08: 빈 저장소 확인. 공식 Sites React/Vinext/TypeScript 템플릿 생성. 기존 제품 코드는 없으며 삭제 대상도 없음.
1. 도메인과 Mock 계약 및 로컬 영속성
2. 모바일 6개 화면의 연결된 흐름
3. 오류/회귀 테스트와 프로덕션 빌드
4. 실제 배포 및 제출 문서
완료는 화면, 로딩·빈·오류 상태, 저장·새로고침, 회귀 검증까지 통과한 경우만 선언한다.

## 구현 상태 갱신
2026-09-09: D1 서버 저장, 방문자 세션 분리, 동시 수정 충돌 방지, 기존 로컬 기록 가져오기, AI JSON/명령 검증과 비동기 Provider 구현. 테스트 21개/타입/lint 통과. 브라우저 주요 흐름 확인. Sites 버전 2 비공개 배포와 운영 저장/새로고침 검증 완료. 실제 AI/OCR과 공개 심사용 접근은 미완료.




## 대표 경험과 상품 의미 해석 · 제출 완성도 개선

냉로그는 사용자가 재고 목록을 계속 정리하는 도구보다, 오늘 확인할 일과 그 이유를 먼저 제안하는 냉장고 관리 에이전트를 지향한다. 현재 판단은 규칙 기반 Mock이며 실제 AI로 표시하지 않는다.

홈의 대표 경험은 오늘의 브리핑 → 해당 재료 상세 → 소비/보관 확인 → 저장 → 새 브리핑이다. 예상 시점이 지난 재료는 먼저 상태 확인 대상으로 안내한다. 메뉴 수 증가보다 이 흐름의 명확성과 안정성을 우선한다.

구매 인식의 핵심은 OCR 문자열 보관이 아니라 상품 의미를 관리 단위로 바꾸는 것이다. 원본 상품, 정규화 이름, 브랜드, 포장 수량/중량, 보관 후보와 불확실성을 보존하고 사용자가 확인한 뒤 등록한다. 완제품은 구성 식재료로 임의 분해하지 않는다. src/product.ts와 app/product-review.tsx에 구현했다.

## 서버 AI 연결 준비 점검

UI의 AI 요청은 모두 /api/ai를 거친다. 동일 Mock/실제 Provider 계약과 JSON Schema, 부분 인식 결과, 서버 이미지 검사, 서버 전용 환경설정 경계를 구현했다. 실제 Provider는 selectProvider에 추가할 어댑터만 비어 있으며 UI/DB/도메인 교체는 필요하지 않다. 실제 API/키/모델은 연결하지 않았다.

## 대회 제한 체험 준비

서버 비용 예약/정산, 익명 체험 횟수, 요청 상한, 결과 캐시/중복 방지, 보호된 비용 상태를 구현했다. 유료 상한27,000원과 Provider 약30,000원 이중 차단을 목표로 하며 실제 가격·환율·전용 Project hard limit 확인 없이는 활성화하지 않는다. Mock0원 별도 장부. 실제 API/키/모델은 미연결이다. 다음은 이미지 픽셀·메타데이터 처리와 비공개 데모 검증이다.

## 2026-09-12 · 영수증 입력과 품목 확인

- 홈/구매 추가에서 영수증 촬영하기와 사진 업로드하기를 분리했다. 촬영만 capture=environment, 업로드에는 capture 없음. 두 input은 같은 ReceiptInput onSelect → MIME/5MiB 검증 → encodeImage → /api/ai 경로다. JPG/JPEG/PNG/WebP를 지원한다. 카메라 열림은 기기/브라우저에 따라 파일 선택으로 대체될 수 있다.
- src/receipt-resolution.ts: 원본 보존, 표시 가격 분리, FOOD/NON_FOOD/UNCERTAIN, 최대3품목/각1.5초/재시도0의 로컬 후보 탐색. 휴지/샴푸/세제/건전지는 제외 목록에 남기고 복원 가능하다. 애매한 품목은 후보 선택/직접 입력/명시 제외 전까지 일괄 등록을 막는다.
- resolution(score 0~1, classification, method, evidence, candidates)와 excluded를 기존 v1 계약의 optional 확장으로 추가. 이전 snapshot은 호환된다. Mock score는 보정된 확률이 아니며 실제 검색처럼 표시하지 않는다. 임계값0.7/0.9는 CONFIDENCE_THRESHOLDS 상수. 높은 점수라도 최종 확인 후 등록한다(첨부 지시의 최종 확인 흐름 우선).
- 원본명/식품명/분류/수량/보관/제품 표시 소비기한을 수정할 수 있다. expiryDate가 있으면 예상 D-day와 보관 변경에서도 제품 표시 날짜를 유지한다. 없으면 기존 데모 예상기간을 사용하며 실제 소비기한을 생성했다고 주장하지 않는다.
- 실제 OCR/Vision/AI 검색은 아직 연결하지 않았다. 이미지 Mock은 식품4개·비식품1개·애매한품목1개 예시다. 잘못된 영수증/흐림 여부를 실제로 판별한다고 주장하지 않는다.
