# Documentation template only. Never place real values in Git.
# Cloudflare local runtime: copy these names into ignored .dev.vars.
AI_PROVIDER=mock
AI_TIMEOUT_MS=15000
# Leave empty until a server provider adapter is implemented and authorized.
AI_MODEL=
AI_API_KEY=
