# 제품 명세

## 사용자 흐름
데모 재고로 시작 → 구매 추가(영수증/온라인 캡처/직접 입력) → 모의 분석 → 이름·수량·단위·구매일·보관 확인/수정 → 등록 → 홈 우선순위 → 자연어 명령 확인 → 재고와 거래 원장 동시 갱신.
이미지는 실제로 해석하지 않는다. 선택 이미지의 미리보기와 고정 샘플 결과를 명확히 구분한다. 분석 요청 시 이미지 바이트를 서비스 서버로 전송하며 영구 저장하지 않는다. Mock에서는 외부 AI로 전송하지 않는다.

## 화면
- 홈: 오늘의 브리핑, 우선 재료, 전체/임박 수, 보관 추천, 구매 추가.
- 냉장고: 전체/냉장/냉동/실온/곧 소비 필터 및 상세 진입.
- 상세: 정보, 예상 시점, 소비/폐기/수량 조정/보관 변경.
- 구매 추가: 입력 유형, 이미지 검증, 분석 상태, 수정 가능한 확인, 중복 방지.
- 도우미: 자연어를 구조화한 명령으로 변환. 실행 전 대상/변경량 확인. 모호한 대상은 실행 금지.
- 기록: 소비/폐기/보관/조정 거래, 재고 초기화 확인.

## 데이터 및 조건
User는 브라우저 데모 사용자. Purchase는 출처/일자/분석 ID와 연결된다. InventoryItem은 ID/이름/상품명/분류/수량/단위/구매일/등록일/보관/예상일을 가진다. InventoryTransaction은 구매·소비·폐기·조정·보관 변경을 보존한다. AIAnalysis는 provider=mock, 입력 유형, 결과, 시각을 가진다.
영속성은 D1의 방문자별 snapshot과 revision. HttpOnly 익명 세션으로 다른 방문자와 분리한다. 서버가 수량과 원장을 함께 저장한다. 기존 localStorage는 한 번 가져오고 백업으로 보존한다. 계정 복구/기기 간 동기화는 미구현이다.
0개 재고는 원장과 함께 보존하되 활성 목록에서 제외. 음수/비수/초과 소비 금지. 같은 명령 중복 실행 방지. 날짜는 서버와 브라우저 모두 Asia/Seoul 달력일 기준. 보관 변경은 구매일 기반으로 재계산하되 기존 만료 재료의 수명을 연장하지 않는다.
예상 기간은 데모 정책으로 식품 안전 보장이 아니며 실제 제공 전 근거 검토가 필요하다. 지난 시점은 섭취 추천 대신 상태 확인 대상으로 분리한다.

## 예외와 완료 기준
이미지 형식·크기, 알 수 없는 식재료, 누락 수량/날짜, 중복 구매, JSON/저장 오류, 0개 소비, 복수 대상, 초과 수량을 처리한다. Mock 해석에는 외부 AI가 필요 없지만 재고 조회·저장은 서버 연결이 필요하다. 네트워크 실패 시 성공으로 표시하지 않고 다시 연결을 안내한다. 저장 실패는 성공으로 표시하지 않는다.
대표 검증: 구매 확인 → 계란 10개 등록 → '계란 3개 썼어' 확인 실행 → 7개 → 닭가슴살 냉동 변경 → 기간 갱신 → 새로고침 유지.

## 서버 동작 조건
GET /api/inventory는 세션별 초기 데모 또는 현재 냉장고를 반환한다. POST는 revision과 command/purchase/reset/import를 받아 서버에서 재검증한다. 오래된 revision은 409, 세션 누락은 401, 다른 Origin은 403, 잘못된 JSON은 400, 1MiB 초과는 413. 동일 구매/소비 ID 재시도로 수량이 두 번 변경되지 않는다.

## 검증 현황
2026-09-09 테스트 21개 통과. 실제 SQLite 격리/동시성/가져오기와 HTTP 보호 조건 포함. 로컬 브라우저에서 구매 등록, 자연어 소비, 새로고침 유지, 냉동 변경 및 한국 날짜 표시 확인. 전체 E2E 자동 회귀는 후속 과제다.


## 상품 의미 해석 계약 v1

흐름: OCR/비전 인식(현재 이미지 경로는 고정 예시) → 상품 의미 해석 → 식재료 정규화 → 상품별 수정/확인 → 서버 검증 → 재고 등록.

Draft.productName은 수량 표기를 포함한 원본 상품 문자열이다. Draft.name은 정규화된 관리 이름이며 meaning.normalizedFoodName과 일치한다. category, quantity, unit, purchasedAt, storage는 기존 필드를 유지한다. meaning에는 version, brand, weightPerUnit, weightUnit(g/kg/ml/L), totalWeight, packaging, storageCandidates(냉장/냉동/실온), processed, openingSensitive, confidence, reasons, confirmed를 저장한다. 미확인 값은 null이며 확정 추론으로 꾸미지 않는다.

- 하림 닭가슴살 블랙페퍼 100g 5팩 → 닭가슴살, 브랜드 하림, 육류, 5팩, 팩당100g, 총500g, 가공품 예시. 보관 후보는 제품 표시보다 우선하지 않는다.
- 닭가슴살 샐러드 1팩 → 완제품 닭가슴살 샐러드 1팩. 닭가슴살/채소/드레싱으로 임의 분해 금지.
- Mock은 confidence=low와 규칙 기반 근거를 제공한다. 모든 신규 의미 해석 결과는 confirmed=false부터 시작한다. 사용자가 이름·수량·보관·구매일을 수정하면 확인을 해제하고 총량을 다시 계산한다.
- 서버 purchase는 미확인 meaning과 총량 불일치를 거절한다. 기존 meaning 없는 데이터는 호환을 위해 계속 읽고 처리한다.
- totalWeight는 구매 당시 총량이다. 소비 후 남은 중량으로 표시하지 않는다. 의미 메타데이터는 D1 스냅샷과 분석 기록에 보존한다.
- 개봉 전후 구분 필요 여부만 수집한다. 현재 실제 개봉 상태/개봉일 기록과 개봉 기반 기간 재계산은 구현하지 않는다.

## 브리핑 개선

대표 CTA는 일반 도우미로 이동하는 대신 우선 대상 재료 상세를 연다. 예상 시점이 지난 재료가 섞여 있으면 상태 확인을 먼저 안내하고 메뉴 제안은 보류한다. 빈 재고에서는 구매 추가로 이어진다. 보관 변경 제안은 제품 표시 확인을 우선하며 자동 변경하지 않는다.

## AI 서버 계약 갱신

POST /api/ai(operation=config/analyze/interpret/briefing)는 동일 Origin과 기존 익명 세션을 요구한다. 재고 상태는 D1에서 읽으며 브라우저가 제공한 임의 상태는 사용하지 않는다. 이미지 분석 입력은 MIME/base64, 원본5MiB/요청7MiB 제한과 파일 헤더 검증을 거친다. 원본 이미지는 서비스 서버에서 요청 동안만 사용하고 DB/로그에 남기지 않는다.

분석 결과는 {version:1,rows,unresolved,warnings}. 새 Provider 응답에서 meaning은 필수이며 confirmed는 항상 false로 재설정한다. 잘못된 구조는 전체 실패, 읽지 못한 상품은 unresolved로 명시한다. 모든 실패를 샘플 성공으로 대체하지 않고 재시도/직접 입력으로 안내한다. 브리핑만 기본 규칙 fallback을 제공한다.

저장 분석의 provider는 mock/remote/fallback을 수용하며 현재 등록 시 서버 설정 모드를 기록한다. 원본 개별 분석 호출과 등록 요청을 연결하는 서명된 분석 ID는 아직 없다. 현재 값은 감사용 출처 증명이 아니다.

## 대회 AI 제한 체험

분석4회/자연어15회(익명 세션 누적), 브리핑5회/한국 날짜. 실패도 횟수에 포함하며 동일 검증 결과24시간 재조회는 무료다. Mock은 별도0원 장부로 동일 제한 흐름을 검증한다. 전체 유료비용은 서버가 최대값을 선예약하며20,000/25,000원 경고,27,000원 초과 요청 차단이다. 기본 조회·수량 수정·필터·날짜 계산·저장은 제한하지 않는다. 한도 도달 시 기존 냉장고 기능을 계속 사용할 수 있음을 안내하고 브리핑 fallback은 기본 규칙이라고 표시한다. 실제 AI는 아직 없다. 가격 확정 후 체험 횟수는 하향 재평가한다.

## 2026-09-12 · 영수증 입력과 품목 확인

- 홈/구매 추가에서 영수증 촬영하기와 사진 업로드하기를 분리했다. 촬영만 capture=environment, 업로드에는 capture 없음. 두 input은 같은 ReceiptInput onSelect → MIME/5MiB 검증 → encodeImage → /api/ai 경로다. JPG/JPEG/PNG/WebP를 지원한다. 카메라 열림은 기기/브라우저에 따라 파일 선택으로 대체될 수 있다.
- src/receipt-resolution.ts: 원본 보존, 표시 가격 분리, FOOD/NON_FOOD/UNCERTAIN, 최대3품목/각1.5초/재시도0의 로컬 후보 탐색. 휴지/샴푸/세제/건전지는 제외 목록에 남기고 복원 가능하다. 애매한 품목은 후보 선택/직접 입력/명시 제외 전까지 일괄 등록을 막는다.
- resolution(score 0~1, classification, method, evidence, candidates)와 excluded를 기존 v1 계약의 optional 확장으로 추가. 이전 snapshot은 호환된다. Mock score는 보정된 확률이 아니며 실제 검색처럼 표시하지 않는다. 임계값0.7/0.9는 CONFIDENCE_THRESHOLDS 상수. 높은 점수라도 최종 확인 후 등록한다(첨부 지시의 최종 확인 흐름 우선).
- 원본명/식품명/분류/수량/보관/제품 표시 소비기한을 수정할 수 있다. expiryDate가 있으면 예상 D-day와 보관 변경에서도 제품 표시 날짜를 유지한다. 없으면 기존 데모 예상기간을 사용하며 실제 소비기한을 생성했다고 주장하지 않는다.
- 실제 OCR/Vision/AI 검색은 아직 연결하지 않았다. 이미지 Mock은 식품4개·비식품1개·애매한품목1개 예시다. 잘못된 영수증/흐림 여부를 실제로 판별한다고 주장하지 않는다.
